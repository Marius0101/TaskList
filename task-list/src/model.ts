export interface ToDo{
    id: number
    task: string
    isCompleted: boolean
}
export interface PlaceholderProps {
    clientHeight: number;
    clientWidth: number;
    clientY: number;
    clientX: number;
}